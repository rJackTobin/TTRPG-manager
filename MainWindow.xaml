<Window x:Class="TTRPG_manager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TTRPG_manager"
        mc:Ignorable="d"
        Title="MainWindow"
        d:DesignHeight="1080"
        d:DesignWidth="1920"
        ResizeMode="CanMinimize"
        >
    <Grid>
        <Grid.Background>
            <ImageBrush x:Name="background" Stretch="UniformToFill"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <!-- This row takes up 1/30th of the height -->
            <RowDefinition Height="29*" />
            <!-- The rest of the space -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <!-- Takes up the full width -->
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Column="0" Grid.Row="0" >
            <Viewbox>
                <StackPanel Orientation="Horizontal">
                    <Button Width="100" Click="SettingsButton_Click">Settings</Button>
                    <Button Width="100" Click="EditMode_Click">Edit Mode</Button>
                </StackPanel>
            </Viewbox>
        </StackPanel>
    </Grid>
</Window>
